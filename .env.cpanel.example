# ========================================
# 🚀 CONFIGURATION PRODUCTION - CPANEL
# ========================================
# Platform: Hostinger cPanel
# Domain: kaolackcommune.sn
# Instructions: Copy this to .env and update values

# Frontend (React/Vite)
VITE_API_URL=https://api.kaolackcommune.sn/api
VITE_APP_NAME=Kaolack Stories Connect
VITE_APP_VERSION=1.0.0

# Backend (Node.js/Express)
NODE_ENV=production
PORT=3001
HOST=127.0.0.1

# Base de données MySQL
# Get these from cPanel: MySQL Databases section
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_cpanel_username_here
DB_PASSWORD=your_secure_password_here
DB_NAME=your_database_name_here
DB_CHARSET=utf8mb4
DB_TIMEZONE=+00:00

# JWT & Sécurité
# Generate secrets with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_WITH_STRONG_SECRET_openssl_rand_-base64_32
JWT_EXPIRY=7d
BCRYPT_ROUNDS=10

# Upload Files
UPLOAD_DIR=/home/your_cpanel_username/backend/uploads
MAX_FILE_SIZE=50000000

# Email (pour notifications)
# Optional - leave as-is if not using email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=noreply@kaolackcommune.sn

# URL Publique
APP_URL=https://kaolackcommune.sn
API_URL=https://api.kaolackcommune.sn
FRONTEND_URL=https://kaolackcommune.sn

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# CORS
CORS_ORIGIN=https://kaolackcommune.sn

# Session
SESSION_SECRET=CHANGE_ME_WITH_STRONG_SECRET_openssl_rand_-base64_32
SESSION_TIMEOUT=86400

# Sentry/Monitoring (optionnel)
SENTRY_DSN=

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# ========================================
# IMPORTANT SETUP INSTRUCTIONS:
# ========================================
# 
# 1. Database credentials (from cPanel):
#    - Go to cPanel > MySQL Databases
#    - Create new database: kaolack_db
#    - Create new user: kaolack_user
#    - Add user to database with all privileges
#    - Update DB_USER, DB_PASSWORD, DB_NAME above
#
# 2. Secrets generation:
#    Run in terminal: openssl rand -base64 32
#    Replace JWT_SECRET with generated value
#    Replace SESSION_SECRET with generated value
#
# 3. Paths:
#    - Replace "your_cpanel_username" with your actual username
#    - UPLOAD_DIR path must exist and be writable
#
# 4. Email (Optional):
#    - If using Gmail, enable "App Passwords"
#    - Or use your email provider's SMTP settings
#    - Leave default if not using email features
#
# 5. SSL/HTTPS:
#    - Automatically enabled via Hostinger AutoSSL
#    - Certificates auto-renew
#
# 6. Test configuration:
#    - After saving, test API:
#    - curl https://api.kaolackcommune.sn/api/health
#
# ========================================
